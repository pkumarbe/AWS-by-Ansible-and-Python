To create a key pair for your project on AWS we need below requirement.
1. Install boto packages.
    pip install botocore
    pip install boto
2. Set the environmetn varibale for authentication.
    export AWS_ACCESS_KEY_ID='xxx'
    export AWS_SECRET_ACCESS_KEY='xx+z'
    export AWS_REGION="us-east-1"

Playbook for create key-pair using ec2 module.

---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Create new ec2 key
      local_action:
        module: ec2_key
        name: my_key
        key_material: 'ssh-rsa xxxxx'
        state: present


# ansible-playbook <palybook.yaml>
 Once the playbook executed successfully the key should be create on the perticular region.
