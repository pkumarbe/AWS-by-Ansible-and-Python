Initial requirement.
1. Make sure boto3 has been installed.

# pip install boto3

2. Create the requied config fils for authentication and config.

# vi ~/.aws/config
  [default]
  region=us-west-1
# vi ~/.aws/credentials
  [default]
  AWS_ACCESS_KEY_ID=xxxx
  AWS_SECRET_ACCESS_KEY=xxxx+z

3. Create the python script as below...

 #vi create_aws_keypair.py
 
  import boto3
  ec2_keys = boto3.client('ec2')
  list_key =  ec2_keys.describe_key_pairs()
  print "List of keys before create" + str(list_key["KeyPairs"])
  create_key = ec2_keys.create_key_pair(KeyName='My_Key')
  list_key =  ec2_keys.describe_key_pairs()
  print "List of keys after create" + str(list_key["KeyPairs"])

4. It should give an out put as below...
  # python create_aws_keypair.py
List of keys before create[]
List of keys after create[{u'KeyName': 'My_Key', u'KeyFingerprint': 'xx:xx:xx:cb:xx:9b:99:14:97:6c:66:c6:8c:07:25:cc:c9:f3:a1:xx'}]
